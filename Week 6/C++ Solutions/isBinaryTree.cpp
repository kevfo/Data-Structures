#include <iostream>
#include <vector>

using std::cout;
using std::cin;
using std::vector;

class Node {
public:
  long val;
  Node* left;
  Node* right;

  void initialize(long val = 0, Node* left = NULL, Node* = right) {
    val = val;
    left = left;
    right = right;
  }
};



int main() {

  return 0;
}
